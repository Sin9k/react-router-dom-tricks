import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  container: {
    padding: 0,
    border: "none",
    cursor: "pointer",
    height: 400,
    overflow: "hidden",
  },
}));

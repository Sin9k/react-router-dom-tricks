import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  "@global": {
    body: {
      margin: 0,
      backgroundColor: "#F1F1F1",
    },
  },
  container: {
    minWidth: "100vw",
    minHeight: "100vh",
  },
}));
